@model IEnumerable<Customer>

@{
    ViewBag.Title = "ListCustomer";
}

<p>@Model.Count() record(s)</p> <!--help to count the record in the database-->
<table class="table">
    <tr>
        <th>Name</th>
        <th>Username</th>
        <th>Phone</th>
        <th>Gender</th>
        <th>Email</th>
        <th></th>
    </tr>

    @foreach (var c in Model)
    {
        <tr>
            <td>@c.Name</td>
            <td>@c.Username</td>
            <td>@c.PhoneNo</td>
            <td>@(c.Gender == "F" ? "👧🏻  Female" : "👦🏻 Male")</td>
            <td>@c.Email</td>
            <td>
                @*<a href="~/Home/Detail?id=@s.Id">Detail</a>*@
                <button data-get="/Admin/Customrdetail?username=@c.Username">Detail</button>
                <button data-get="/Admin/CustomerEdit?username=@c.Username">Edit</button>
                <button data-get="/Admin/CustomerDelete?username=@c.Username">Delete</button>
            </td>

        </tr>
    }
</table>


